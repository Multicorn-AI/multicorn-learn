/**
 * Learn site feature flags from multicorn-service.
 * Keep `IN_USE_FLAGS` in multicorn-service (`InUseFeatureFlags.kt`) in sync.
 */
export const LEARN_LAUNCH_FEATURE_FLAG = 'learn_launch' as const
